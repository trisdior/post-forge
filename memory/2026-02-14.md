# 2026-02-14 Session Notes

## Email Delivery — Key Findings
- **Brevo API key is INVALID**: [REDACTED - INVALID KEY, SAFE TO LEAVE]
- The `xsmtpsib-` prefix is an **SMTP key**, NOT a v3 API key — need Tris to get the correct v3 API key from Brevo dashboard (Settings → SMTP & API)
- WP Mail SMTP plugin had a secondary issue: password fields use `data-name` not `name`, and are `disabled` — must click "Remove API Key" first to unlock the field before re-entering
- Bluehost blocks ALL outgoing SMTP (ports 587, 465, 25) — confirmed Brevo SMTP relay (`smtp-relay.brevo.com:587`) also blocked
- Only viable path: Brevo HTTP API via `wp_remote_post()` to `https://api.brevo.com/v3/smtp/email`

## WPCode Snippets Created
1. **"Brevo Email Override"** — uses phpmailer_init to set SMTP relay (WON'T WORK due to Bluehost port blocking) — should be DEACTIVATED
2. **"Brevo HTTP API Mail"** — uses `pre_wp_mail` filter to send via Brevo HTTP API — CORRECT approach, but needs valid API key

## Plugin Inventory (active)
- Formidable + Formidable Pro (form builder)
- WP Mail SMTP (email, mailer=sendinblue/brevo)
- WPCode / Insert Headers & Footers (code snippets — ACTIVE, works)
- Jetpack, Jetpack Boost, Jetpack Protect, Jetpack CRM
- Google Site Kit, MonsterInsights
- OptinMonster, Creative Mail by Constant Contact
- AIOSEO, Broken Link Checker
- Bluehost plugin
- Code Snippets plugin NOT installed (404 on admin page)

## Next Steps
1. Get correct Brevo v3 API key from Tris
2. Update the "Brevo HTTP API Mail" WPCode snippet with correct key
3. Disable the "Brevo Email Override" SMTP snippet (it can't work)
4. Test email send
5. Then test Formidable form submission end-to-end
