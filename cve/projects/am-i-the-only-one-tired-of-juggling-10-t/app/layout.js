export const metadata = {
  title: 'Am I the only one tired of juggling 10 tools to build one startup? I will not promote.',
  description: 'Solving: Am I the only one tired of juggling 10 tools to build one startup? I will not pr',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
