export const metadata = {
  title: 'Went from $0 to $1k MRR. If I started my SaaS over, here's exactly what I'd do',
  description: 'Solving: Went from $0 to $1k MRR. If I started my SaaS over, here's exactly what I'd do',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
